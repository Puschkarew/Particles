# 🎮 Интеграция сцены Kefermarkt Church (Gaussian Splatting)

## 📋 Инструкция по извлечению и использованию сцены

### Шаг 1: Извлечение файлов из PlayCanvas

#### Вариант А: Через DevTools (Рекомендуется)

1. **Откройте оригинальную сцену**
   - Перейдите на https://playcanv.as/p/Zp9Oh1ia/
   - Дождитесь полной загрузки сцены

2. **Откройте Developer Tools**
   - Windows/Linux: `F12` или `Ctrl + Shift + I`
   - Mac: `Cmd + Option + I`

3. **Перейдите на вкладку Network**
   - Обновите страницу (`F5`)
   - В фильтре введите: `.sog` или `.ply`

4. **Найдите файлы Gaussian Splat**
   - Ищите файлы с расширениями:
     - `.sog` - Scene Organized Gaussian Splat (основной файл)
     - `.ply` - Point Cloud данные
     - `.glb` - Дополнительные 3D модели
     - `.json` - Метаданные сцены

5. **Скачайте файлы**
   - Правый клик на файле → Copy → Copy link address
   - Откройте URL в новой вкладке
   - Браузер предложит скачать файл

#### Вариант Б: Через Console Script (Автоматический)

1. **Откройте Console** (вкладка Console в DevTools)

2. **Вставьте и выполните скрипт** `extract-playcanvas-assets.js`
   ```javascript
   // Скопируйте содержимое файла extract-playcanvas-assets.js
   // и вставьте в Console
   ```

3. **Используйте команды**
   ```javascript
   // Скачать все ресурсы
   downloadAllAssets()
   
   // Экспортировать список URL
   exportAssetList()
   
   // Получить информацию из PlayCanvas API
   getPlayCanvasAssets()
   ```

### Шаг 2: Организация файлов

Создайте следующую структуру папок в вашем проекте:

```
reveal/
├── assets/
│   ├── kefermarkt/
│   │   ├── church.sog          # Основной файл Gaussian Splat
│   │   ├── metadata.json       # Метаданные (опционально)
│   │   └── textures/           # Текстуры (если есть)
├── kefermarkt-church.html      # Основной файл
└── README.md                   # Эта инструкция
```

### Шаг 3: Настройка HTML файла

Откройте `kefermarkt-church.html` и обновите URL файла:

```javascript
// Найдите строку:
const sogUrl = 'https://playcanv.as/api/assets/files/your-file-here.sog';

// Замените на:
const sogUrl = 'assets/kefermarkt/church.sog';
```

### Шаг 4: Запуск локального сервера

Из-за CORS политик, файл нужно открывать через веб-сервер:

#### Python 3
```bash
cd reveal
python -m http.server 8000
```

#### Python 2
```bash
cd reveal
python -m SimpleHTTPServer 8000
```

#### Node.js (http-server)
```bash
npm install -g http-server
cd reveal
http-server -p 8000
```

#### PHP
```bash
cd reveal
php -S localhost:8000
```

Затем откройте браузер и перейдите на: http://localhost:8000/kefermarkt-church.html

### Шаг 5: Альтернативные подходы

#### Метод 1: Использование CDN

Если файл слишком большой для хостинга, используйте CDN:

```javascript
// Загрузите файл на:
// - GitHub (через GitHub Pages)
// - Cloudflare R2
// - AWS S3
// - Google Cloud Storage

const sogUrl = 'https://your-cdn.com/kefermarkt/church.sog';
```

#### Метод 2: Прямая ссылка на PlayCanvas

```javascript
// Используйте прямую ссылку (если CORS позволяет)
const sogUrl = 'https://playcanv.as/api/assets/files/[FILE_ID]/church.sog';
```

#### Метод 3: Proxy сервер

Если CORS блокирует загрузку, используйте proxy:

```javascript
const sogUrl = 'https://cors-anywhere.herokuapp.com/https://playcanv.as/...';
```

### Шаг 6: Кастомизация

#### Изменить начальную позицию камеры

```javascript
camera.setPosition(0, 2, 8); // x, y, z
```

#### Изменить цвет фона

```javascript
camera.addComponent('camera', {
    clearColor: new pc.Color(0.2, 0.3, 0.4), // RGB (0-1)
    // ...
});
```

#### Добавить больше освещения

```javascript
const pointLight = new pc.Entity('point-light');
pointLight.addComponent('light', {
    type: 'point',
    color: new pc.Color(1, 0.9, 0.8),
    intensity: 2,
    range: 10
});
pointLight.setPosition(0, 3, 0);
app.root.addChild(pointLight);
```

## 🔧 Решение проблем

### Проблема: Файл не загружается

**Решение 1:** Проверьте путь к файлу
```javascript
// Используйте относительный путь
const sogUrl = './assets/kefermarkt/church.sog';
```

**Решение 2:** Проверьте CORS
- Убедитесь, что используете локальный сервер
- Проверьте настройки CORS на вашем хостинге

### Проблема: Черный экран

**Решение:** Проверьте консоль браузера (F12)
- Ищите ошибки загрузки
- Убедитесь, что PlayCanvas загружен
- Проверьте правильность пути к файлу

### Проблема: Низкая производительность

**Решение 1:** Уменьшить качество
```javascript
app.setCanvasResolution(new pc.Vec2(1280, 720));
```

**Решение 2:** Отключить тени
```javascript
light.addComponent('light', {
    castShadows: false // Отключить тени
});
```

## 📦 Список извлекаемых файлов

### Обязательные файлы:
- `*.sog` или `*.ply` - Основные данные Gaussian Splat
- `metadata.json` - Метаданные сцены (размеры, позиция и т.д.)

### Опциональные файлы:
- `*.glb` - Дополнительные 3D модели
- `*.jpg`, `*.png` - Текстуры
- `*.hdr` - HDR окружение для освещения
- `*.json` - Конфигурация сцены

## 🎨 Дополнительные возможности

### Добавить UI элементы

```javascript
// Добавить кнопку в HTML
<button onclick="changeView(1)">Вид 1</button>
<button onclick="changeView(2)">Вид 2</button>

// В JavaScript
function changeView(num) {
    if (num === 1) {
        camera.setPosition(0, 1.5, 5);
        camera.lookAt(0, 1, 0);
    } else if (num === 2) {
        camera.setPosition(5, 2, 0);
        camera.lookAt(0, 1, 0);
    }
}
```

### Добавить анимацию камеры

```javascript
let time = 0;
app.on('update', (dt) => {
    time += dt;
    camera.setPosition(
        Math.sin(time * 0.5) * 5,
        2,
        Math.cos(time * 0.5) * 5
    );
    camera.lookAt(0, 1, 0);
});
```

### Добавить постобработку

```javascript
camera.addComponent('camera', {
    clearColor: new pc.Color(0.1, 0.1, 0.1),
    // Добавить bloom эффект
    bloom: true,
    bloomIntensity: 0.3
});
```

## 📚 Дополнительные ресурсы

- [PlayCanvas Documentation](https://developer.playcanvas.com/)
- [Gaussian Splatting Overview](https://developer.playcanvas.com/user-manual/graphics/gaussian-splatting/)
- [PlayCanvas Examples](https://playcanvas.github.io/)

## 📄 Лицензия

Сцена "Kefermarkt Church" создана Schindelar3D в сотрудничестве с Fotomento.  
Используйте в соответствии с разрешением автора.

## 🤝 Поддержка

Если у вас возникли вопросы:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что все файлы загружены
3. Проверьте пути к файлам
4. Убедитесь, что используете локальный сервер

---

**Примечание:** Файлы Gaussian Splat могут быть большого размера (100+ MB).  
Убедитесь, что у вас достаточно места на диске и пропускной способности интернета.

